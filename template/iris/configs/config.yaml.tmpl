service:
  host: {{.Host}}
  port: {{.Port}}

mysql:
  host:
  port:
  user:
  password:
  db:
  log_mode:

redis:
  host:
  port:
  password:
  db:
  prefix: {{.ModPrefix}}{{.Name}}

schedule:
  demo:
    rule: "*/5 * * * *"
    manual_trigger: false

log_path: {{.LogPath}}
